"""
Joblib wrapper for model artifact persistence.

Load/save pipeline and metadata JSON.
"""

import joblib
import json
from pathlib import Path
from typing import Any, Dict


# TODO: Implement artifact manager
# - save_pipeline(pipeline, filepath)
# - load_pipeline(filepath)
# - save_metadata(metadata_dict, filepath)
# - load_metadata(filepath)
